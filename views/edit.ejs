<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootswatch@5.3.3/dist/darkly/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="container py-4">
  <h1 class="mb-4"><%= title %></h1>
  <form action="/edit/<%= movie.id %>" method="POST" class="mb-3">
    <div class="mb-2">
      <label class="form-label">Tytuł:</label>
      <input type="text" name="title" value="<%= movie.title %>" class="form-control" required>
    </div>
    <div class="mb-2">
      <label class="form-label">Reżyser:</label>
      <input type="text" name="director" value="<%= movie.director %>" class="form-control" required>
    </div>
    <div class="mb-2">
      <label class="form-label">Ocena:</label>
      <input type="number" name="rating" value="<%= movie.rating %>" class="form-control" min="1" max="10" required>
    </div>
    <div class="mb-2">
      <label class="form-label">Status:</label>
      <select name="status" class="form-select">
        <option value="obejrzane" <%= movie.status === 'obejrzane' ? 'selected' : '' %>>Obejrzane</option>
        <option value="w trakcie" <%= movie.status === 'w trakcie' ? 'selected' : '' %>>W trakcie</option>
      </select>
    </div>
    <div class="mb-3">
      <label class="form-label">Recenzja:</label>
      <textarea name="review" rows="3" class="form-control"><%= movie.review %></textarea>
    </div>
    <button type="submit" class="btn btn-primary">Zapisz zmiany</button>
  </form>
  <a href="/home" class="btn btn-outline-secondary">← Powrót</a>

</body>
</html>
